import { flag } from "../../../utils/anticheat/punishment/flag.js";
import config from "../../../data/config.js";

// Ideas:
// Check for bad randomization
export function autoclicker_d(player) {
    return;
}